package com.mustafakoceerr.core.theme.model

/**
 * Represents the available theme modes for the application.
 */
enum class AppThemeMode {
    /** Follows the system's theme setting. */
    SYSTEM,

    /** Forces the application to use the light theme. */
    LIGHT,

    /** Forces the application to use the dark theme. */
    DARK
}

